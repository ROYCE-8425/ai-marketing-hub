# AI Marketing Hub — Backend Dependencies (Phase 8)
# Install all: pip install -r requirements.txt

# ── Core Framework ──────────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# ── Core Runtime (required by router modules) ───────────────────────────────────
requests>=2.31.0           # wordpress_publisher, httpx (also used directly)
httpx[http2]>=0.27.0       # URL scraping (api_seo, api_data)
beautifulsoup4>=4.12.0     # HTML parsing (api_seo, api_data)
lxml>=5.0.0                # Fast HTML parser for beautifulsoup4

# ── Core Analysis (Phase 1/3 analyzers) ────────────────────────────────────────
scikit-learn>=1.3.0        # keyword clustering in keyword_analyzer
numpy>=1.24.0               # numeric support for sklearn

# ── Phase 7 — Content Polish ────────────────────────────────────────────────────
textstat>=0.7.0             # readability scoring (anti-detection)
markdown>=3.5                # Markdown-to-HTML for WordPress publisher (graceful fallback)

# ── Phase 8 — Live SERP Results ─────────────────────────────────────────────────
duckduckgo-search>=7.0.0    # real search results (free, no API key)

# ── Optional Live-Data Connectors (Phase 6) ─────────────────────────────────────
# These are lazy-loaded: server starts without them but endpoints will return
# mock data unless credentials are configured via env vars.
#
# Google Search Console + GA4:
#   GOOGLE_SEARCH_CONSOLE_CLIENT_ID, GOOGLE_SEARCH_CONSOLE_CLIENT_SECRET,
#   GOOGLE_SEARCH_CONSOLE_REFRESH_TOKEN, GSC_SITE_URL
#   GA4_PROPERTY_ID, GA4_CREDENTIALS_PATH (service-account JSON)
# google-analytics-data>=0.18.0
# google-api-python-client>=2.100.0
# google-auth-httplib2>=0.1.0
# google-auth>=2.23.0
#
# DataForSEO SERP data:
#   DATAFORSEO_LOGIN, DATAFORSEO_PASSWORD
# dataforseo>=1.0.0

# ── Phase 20 — MarkItDown File Converter ────────────────────────────────────────
markitdown[all]>=0.1.5       # Microsoft's file-to-markdown converter (PDF/Word/Excel/PPT)
python-multipart>=0.0.9       # Required for FastAPI file uploads

# ── Phase 21 — Excel Export ─────────────────────────────────────────────────────
openpyxl>=3.1.0              # XLSX writer for rank tracker / reports export
